import { describe, it } from 'vitest';

describe.skip('Agenda smoke', () => {
  it('fetches classes/stats with org scoping', async () => {
    // TODO: exercise GET /api/agenda/classes with x-organization-id
  });
});

