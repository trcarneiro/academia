import { describe, it } from 'vitest';

describe.skip('Units smoke', () => {
  it('lists units by org and gets unit only if belongs', async () => {
    // TODO: exercise GET /api/units with x-organization-id
  });
});

