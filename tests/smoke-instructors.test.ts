import { describe, it } from 'vitest';

describe.skip('Instructors smoke', () => {
  it('lists instructors by org and prevents cross-org access', async () => {
    // TODO: exercise GET /api/instructors with x-organization-id
  });
});

