import { describe, it } from 'vitest';

describe.skip('Courses smoke', () => {
  it('imports course for org only', async () => {
    // TODO: exercise POST /api/courses/import with x-organization-id
  });
});

